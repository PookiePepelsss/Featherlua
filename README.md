# Luau Compressor Web

A small browser-only Lua/Luau source compressor. Paste code, select **Compress**,
then copy or download the result. Source code stays on the device.

```powershell
npm install
npm run dev
```

Open `http://localhost:3000`.
